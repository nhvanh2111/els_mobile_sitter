enum AttitudeType { uncooperative, unFriendly, other }

enum CusInfoType { missing, wrong, other }

enum InfoBookingType { address, elder, package }

enum CancelBookingType { time, cancel }

enum WorkingTimeType{date, week, month}

class MyEnum {}
